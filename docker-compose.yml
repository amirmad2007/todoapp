
version: "3.9"
services:
  backend:
    build: .
    env_file:
      - .env  
    container_name: todoapp
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
